image:
  - Visual Studio 2015
build_script:
  # windows
  - cmd: pip install cibuildwheel==1.1.0 scikit-build
  - cmd: cibuildwheel --output-dir wheelhouse
artifacts:
  - path: "wheelhouse\\*.whl"
    name: Wheels
